<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Orders</title>

    <!-- Link to Bootstrap CSS (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 900px;
            margin-top: 50px;
        }

        .page-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 600;
            color: #333;
        }

        .table th,
        .table td {
            vertical-align: middle;
            text-align: center;
        }

        .table td {
            font-size: 1.1rem;
        }

        .table thead {
            background-color: #343a40;
            color: white;
        }

        .table tbody tr:nth-child(even) {
            background-color: #f8f9fa;
        }

        .badge {
            font-weight: bold;
        }

        .table-container {
            background-color: #ffffff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-radius: 8px;
        }

        .btn-complete {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            font-size: 1rem;
            padding: 10px 20px;
            border: none;
        }

        .btn-complete:hover {
            background-color: #218838;
        }

        .btn-view {
            background-color: #007bff;
            color: white;
            border-radius: 5px;
            font-size: 1rem;
            padding: 10px 20px;
            border: none;
        }

        .btn-view:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="page-header">
            <h1>Pending Orders</h1>
            <p>Below are the orders that are yet to be completed.</p>
        </div>

        <div class="table-container">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Order #</th>
                        <th>Unique Id</th>
                        <th>Order Date</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach((order) => { %>
                        <% if (!order.isPaid) { %>
                            <tr>
                                <td><%= order.id %></td>
                                <td><%= order.shortId %></td>
                                <td><%= order.createdAt %></td>
                                <td>
                                    <div class="row ms-1">
                                        <!-- View Button -->
                                        <button onclick="document.location = '/orders/<%= order.id %>'"
                                            class="btn btn-view col mb-1">View</button>

                                        <!-- Pay Button -->
                                        <form action="/orders/<%= order.id %>" method="post" class="col">
                                            <button type="submit" class="btn btn-danger">Pay</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <% } %>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Link to Bootstrap JS (CDN) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
